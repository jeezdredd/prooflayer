# Hot Cache

Fast-load context for next session. Read me first.

## Last touched

- 2026-09-15: Prod media broken after private-media switch: presigned URLs signed for `http://minio:9000` (compose hostname) and Caddy rewrote path+Host, killing SigV4. Fix: `content.storage.PublicSignedS3Storage` signs against `AWS_S3_PUBLIC_ENDPOINT_URL` (default `https://$AWS_S3_CUSTOM_DOMAIN`), Caddy media block no longer rewrites bucket-prefixed paths and passes Host through - **the Caddyfile on the server must be edited by hand** (sudo). Landing Tribunal section: looping icon animations replaced by hover/expand springs. See [[fixes/presigned-media-public-endpoint]].

- 2026-09-04 (d): **Tribunal 1.1 - the retrain.** Ensemble got a name (`analyzers/ensemble.py`: Tribunal, version + roster fingerprint on every submission detail, status probe, landing page section). `custom_detector` retrained from the **community_forensics backbone** on 400 real / 736 AI (openfake + hf), 3 epochs, class-weighted, **130 s on MPS**; held-out on a deduplicated second OpenFake pull (100/145, 20 gens; `dedupe_dataset` removed 13 exact dupes - row-group offset alone was NOT disjoint): AUC **0.711 -> 0.993**, flux.2 0.17->0.91, gpt-image-2 ->0.998, sora-2 ->0.83. Weights were then the cap (caught 35% either way), then the two-corroborator rule was: "any single voter" convicted a real photo on 3 of 4 rosters (retrained model puts 5% of real photos >=0.75). Shipped the share+certainty exception (lone fake voter iff weight share >=50% AND p>=0.9) + weights CF 3.5 / custom 3.5 / ai 1.0 -> **caught 34 -> 103/145 (71%)**, AI->authentic 39 -> 4, **real->fake 0/100**, ensemble AUC 0.916 -> **0.993**, acc 0.776 -> 0.951; diffusiondb 60/60. Cost: real->suspicious 2 -> 8. Still weak: gpt-image-2 1/6, flux.2 2/8, veo-3 2/8, sora-2 2/7 (CF outweighs the member that sees them). Model at `~/.cache/huggingface/prooflayer-retrained/image` (sha 719fc0d5...); `make ship-retrained` (untested vs server) copies into worker `hf_cache`; ROCm worker must be rebuilt for code; `make seed-analyzers-server` for weights. Gotchas: local dev DB was unmigrated (retrain now falls back to --extra-dir); 250 stub files in hf/real would crash Trainer (`_collect_extra` verifies). Suite **406 passed**. See [[concepts/tribunal]], [[analyzers/custom_detector]], [[fixes/audit-2026-08]].

- 2026-09-04 (c): Modern generators measured. `fetch_openfake` (pyarrow row-group streaming; `datasets` streaming OOM-killed on 5 GB shards) pulled 150 real + 236 AI over **20 current generators** from OpenFake (CC-BY-NC). Ensemble called **41% of modern AI "authentic"** (AUC 0.808, acc 0.627) - the benchmark paper reproduced. Diagnosis: CF still ranks (AUC 0.977) but raw sigmoid median 0.32 vs 0.997 on diffusiondb, and **real photos never exceed 0.147 raw** (p99 0.0045) across 210 - so it was calibration, not discrimination. Shipped `community_forensics.calibrate()` piecewise-linear knots `(0.005->0.20, 0.10->0.55, 0.50->0.75)`, validated on a generator-split holdout both ways (real->fake 0 in every split). Also unseeded `siglip_detector` (35% FP on real photos, drove all 7 real `needs_review` -> 0) and `face_deepfake_detector` (AUC 0.181 on OpenFake). Result: ensemble AUC **0.890**, acc **0.777**; modern AI caught 18% -> 27%, called authentic 41% -> 24%, real->fake 0; diffusiondb fake 43 -> 55. Ceiling: flux.2-klein-9b and sora-2 score ~0 raw on CF (0% caught) - need a retrain or a new detector. `MIN_CORROBORATING_FOR_FAKE=2` caps recall when CF is the only strong voter. Active image roster: CF 3.5, custom 1.5, ai_detector 1.5, metadata 1.5, ela 0.75. Suite **376 passed**. See [[fixes/audit-2026-08]], [[analyzers/community-forensics]], [[concepts/detector-evaluation]].

- 2026-09-04 (b): Roster drift made visible. Trigger: `manage.py seed_analyzers` run on the host failed - `.env` points DB/redis at compose hostnames `db`/`redis`, and plain `python` has no Django; the working host recipe is `make seed-analyzers-host` (uv + localhost overrides). Prod seeds automatically on backend boot (`compose.prod.yml:47`), so a worker-only rebuild keeps stale weights silently. Now: `analyzers/roster.py:roster_drift()` diffs DB rows vs `seed_analyzers.ANALYZERS` (missing / inactive_expected / stale_active / class_mismatch / weight_mismatch); `analyzers.W001` system check (skips if table absent); `/system/status` gains an `analyzers` probe (`down` on drift, degrades overall) + StatusPage row "Analyzer Roster"; `seed_analyzers --check` exits 1 on drift; Makefile `seed-analyzers`, `seed-analyzers-host`, `seed-analyzers-server`, `roster-check`. Found while testing: `seed_analyzers` never re-activated a deactivated row (`is_active` missing from defaults) - fixed. Local dev DB had a stale active `clip_detector` row. Suite **357 passed**. See [[api/system-status]], [[analyzers/_index]].

- 2026-09-04: Real NPR implemented and measured out. `npr_detector` had been `Wvolf/ViT_Deepfake_Detection` (face-swap ViT) all along -> moved to `face_deepfake_detector` (0.5). Real NPR (Tan et al. CVPR 2024) built from the authors' repo: stem+layer1+layer2 over the nearest-neighbour resampling residual, 1.44M params, sha256-pinned `model_epoch_last_3090.pth`, no resize, verified on the authors' demo images (+110 / -114 logits). **Then it measured AUC 0.499** on flickr-vs-diffusiondb: `authentic` at p<0.01 on 58/60 AI *and* 57/60 real - ProGAN-trained, blind to diffusion, and saturated so it votes confidently wrong. It pushed 27/60 AI images to `needs_review`. Decision: module + 24 tests + wiki kept, **not seeded**. Root cause underneath: disagreement rule counted heads, so 1.0+0.5 of saturated junk outvoted 3.5+1.5+1.5 -> now weight-share (lighter camp >= 30%). `CF_PRIORITY_PEERS` += custom_detector, ai_detector (only peer before was siglip @ AUC 0.32). Final roster: AI fake 43 / likely 4 / inconclusive 13, real authentic 53 / review 7, ensemble AUC 1.000. Suite **341 passed**. See [[analyzers/npr-detector]], [[fixes/audit-2026-08]].

- 2026-08-19: Full system audit. Test suite 33 failed/133 passed -> **307 passed**. Detection bugs found: aggregator's hardcoded `PROBABILISTIC_ANALYZERS` name-set gave `llm_vision` **zero weight** (in the set, never emits `ai_probability`) while discarding `custom_detector`/`ai_detector` probabilities into coarse buckets - now sourced from evidence keys. `extract_metadata` read IFD0 only, so `ExposureTime`/`FNumber`/`ISO`/`LensModel` were invisible and `has_camera_signature` could **never** fire (verified: real camera JPEG gave only Make/Model) - now merges Exif sub-IFD 0x8769. `video_frame` had `fake` unreachable behind a `>=0.65 suspicious` branch AND fed CPU tensors to a GPU model, so video was fully broken on the ROCm worker; also only saw the first ~8s. `ai_detector` was missing from `seed_analyzers` so the stale-sweep deactivated it (dead code). C2PA never worked: `c2pa.read_file` removed in c2pa-python 0.5+. PNG `parameters`/XMP chunks never parsed. Security: SSRF DNS-rebinding TOCTOU (validate then re-resolve) now pins the validated IP set; 500MB uploads buffered in RAM (`FILE_UPLOAD_MAX_MEMORY_SIZE`); IDOR on `SubmissionStatusView`. Weights rebalanced (custom_detector 3.5->1.5, community_forensics 3.0->3.5, metadata 2.5->1.5). See [[fixes/audit-2026-08]]. **Second pass**: S3 media was `public-read`+`querystring_auth:False` so every upload was world-readable regardless of `is_public` -> `MEDIA_PRIVATE` + presigned (ELA heatmap now stores `heatmap_path`, re-signed in serializer). `WidgetEmbedView` was an unthrottled hash oracle over private submissions -> public-only + 120/hour. Anonymous analyze-url landed in a random staff account -> dedicated inactive `anonymous@prooflayer.invalid`. Retrain: lazy Dataset (was materializing all tensors, GBs), split by source group (8 frames of one video straddled train/eval), `media_type` no longer hardcoded. `aggregate_verdicts` guarded by `cache.add` lock (rescue task raced the chord callback). Audio now emits `ai_probability`. New `manage.py eval_detectors` harness, see [[concepts/detector-evaluation]]. Suite: **313 passed**. **Measured on 120 labelled images** (`eval_detectors`): community_forensics AUC 1.000, custom_detector 0.920, ai_detector 0.914, npr 0.688, **siglip 0.323 (anti-correlated - ranks real photos as more AI-like than AI images)** -> weight 2.0->0.5. **ELA was inverted**: called 38/60 real photos fake-or-suspicious vs 12/60 AI images; it measures recompression locality, not synthesis -> now returns `inconclusive` on the AI axis (so it drops out of the weighted mean) and reports `manipulation_suspected` in evidence. Verdicts on real photos: clean `authentic` 17 -> 53, false `authentic_edited` 21 -> 0, `needs_review` 21 -> 7; ensemble AUC stays 1.000.

- 2026-06-21: Email robustness + audit trail. Root cause of "verification email doesn't work": worker had no RESEND_API_KEY -> base.py silent console fallback -> mail to stdout, no error (env-drift, same class as DB/MinIO). Fix (additive, 10 TDD tasks): `users/emails.py:deliver()` choke point + `EmailLog` model/admin (read-only) audit trail; `EMAIL_CONFIGURED` + `users.W001` system check (warns loud in prod); `_probe_email` in /system/status (PII-free, flags worker drift via recent console/failed rows) + StatusPage row; register/resend `delivery` field + VerifyGate honest message; `manage.py send_test_email`; token-reuse on retry; retrain-notify routed through deliver (swallowed). See [[services/auth-email]]. Immediate ops fix: put RESEND_API_KEY + DEFAULT_FROM_EMAIL + FRONTEND_URL in homelab .env, verify Resend domain, recreate worker.

- 2026-06-20: System audit pass. Security: SSRF redirect re-validation (`common/url_safety.safe_get`, used by factcheck fetch-url + AnalyzeUrlView), generic error messages (no `str(exc)` leak), DOCX zip-bomb + PDF page caps, `PIL.Image.MAX_IMAGE_PIXELS=64M`, prod CORS no-allow-all. ROCm: CLIP provenance `use_safetensors` (re-enables pgvector neighbor search) + defensive flags on siglip/custom_detector. Docs: README 7->10 analyzers + real model ids, new [[analyzers/custom_detector]] + [[services/gpu-rocm]] pages, celery-workers `--pool=solo`/preload/env_file, endpoints retrain, ollama Railway purge, .env.example refreshed. Tests: common/tests (url_safety incl safe_get redirect, throttling), factcheck test_views rewritten for async + export/fetch-url/extract-doc, content null-byte + analyze_url patch targets fixed.

- 2026-06-20: null-byte in EXIF crashed process_submission. `_json_safe` (content/services.py) passed EXIF strings through unchanged; some carry embedded `\x00` (UserComment/GPS/MakerNote). Postgres jsonb/text rejects ` ` -> `psycopg UntranslatableCharacter` on submission.save -> submission failed. Real code bug (existed on CPU too, just never reached until storage/DB/GPU chain was fixed). Fix: `_clean_str` strips `\x00` from every string `_json_safe` returns (str/bytes-decode/dict-keys/str-fallback). Secondary non-fatal: provenance CLIP embedding `torch.load CVE-2025-32434` on torch 2.3 -> clip_embedding stays None (caught), pgvector neighbor search disabled on ROCm; fix later with use_safetensors.
- 2026-06-20: ROCm worker storage drift (3rd env-drift link). Even with AWS creds, worker still hit FileNotFoundError on local /app/media - because it ran `config.settings.dev` (forces FileSystemStorage) while backend runs `config.settings.prod` (S3/MinIO when AWS set; also REQUIRES SECRET_KEY env). Fix: celery_worker now `env_file: .env` (required:false) so every secret in .env injects literally - no `${}` interpolation pain for keys with `$`/special chars - plus `DJANGO_SETTINGS_MODULE=${DJANGO_SETTINGS_MODULE:-config.settings.dev}`. Homelab .env must set `DJANGO_SETTINGS_MODULE=config.settings.prod` + `SECRET_KEY=...`. env_file auto-covers future drift.
- 2026-06-20: ROCm worker env-drift gotcha. The ROCm celery_worker is rebuilt MANUALLY (`WORKER_DOCKERFILE=backend/Dockerfile.rocm docker compose up celery_worker`), NOT via deploy.sh, so it only gets env that lives in `docker-compose.yml` + `.env` - it misses anything deploy.sh injects at runtime. Two failures chased: (1) submissions stuck `pending` = worker had default POSTGRES_PASSWORD, DB auth failed on `Submission.objects.get`. (2) submissions `failed` / all analyzers SKIPPED = worker had NO AWS_*/MinIO creds, so django-storages fell back to local FileSystemStorage and hit FileNotFoundError (files live in MinIO). Fix: forward AWS_ACCESS_KEY_ID/SECRET/BUCKET/ENDPOINT/CUSTOM_DOMAIN + GOOGLE_FACT_CHECK_KEY into celery_worker.environment, and keep real POSTGRES_PASSWORD + AWS creds in `/srv/prooflayer/current/.env` so manual rebuilds interpolate them. Lesson: any secret backend needs, the worker needs too - put them all in .env.
- 2026-06-20: GPU analyzer tasks hung forever in celery prefork worker. Root cause: HIP/ROCm is fork-unsafe (same as CUDA). billiard prefork forks pool child, first `model.to('cuda')` in child blocks on uninterruptible C call -> analyzer #1 stuck RUNNING, pipeline wedged. Non-GPU tasks (DB-only) ran fine in same child (3ms sanity), preload loaded models on cuda in standalone non-forked process -> proved GPU itself works, only fork breaks it. Fix: worker `--pool=solo` (runs task in MainProcess, valid HIP context, no fork) in docker-compose `command:` + Dockerfile.rocm CMD. Dropped concurrency/max-tasks/max-memory (no-op under solo). Alt = `--pool=threads --concurrency=1`. CPU escape hatch: `PROOFLAYER_FORCE_CPU=1` (honored by analyzers/_device.get_device).
- 2026-06-19: ROCm worker bringup gotchas fixed: HF model cache wasn't persisted (rocm/pytorch image has no volume), so every first submission hung the single-concurrency worker for 5-15 min on `from_pretrained` download. Added `hf_cache` named volume mounted at `/root/.cache/huggingface` + `HF_HOME=/root/.cache/huggingface` env. New `analyzers/preload_models` management command pre-pulls siglip / community_forensics / npr / clip ensemble on boot. Docker `command:` chains preload then celery so user submissions never trigger first download. Bumped `CELERY_TASK_TIME_LIMIT` 300->900, `SOFT_TIME_LIMIT` 240->720 to survive cold model loads. Added `--without-heartbeat --without-gossip --without-mingle` to celery to reduce broker chatter (concurrency=1 doesn't need cluster gossip).
- 2026-06-19: GPU/ROCm wiring. Dockerfile gets TORCH_INDEX_URL build arg (default cpu); `celery_worker` in compose mounts /dev/kfd + /dev/dri, group_add video+render, env HSA_OVERRIDE_GFX_VERSION + HIP_VISIBLE_DEVICES. Build worker with `TORCH_INDEX_URL=https://download.pytorch.org/whl/rocm6.0` to get GPU torch. AMD RX 6650 XT (gfx1032) supported on ROCm 6.x. New `analyzers/_device.py` (get_device/to_device/inputs_to_device); siglip_detector / community_forensics / npr_detector / clip_detector now load on cuda when available (ROCm masks as cuda), inputs moved per-call, outputs squeeze().cpu() before further processing. HF Trainer in retrain_detector auto-uses GPU - no code change there.
- 2026-06-19: VerdictOverrideView now marks submission `approved_for_training=True` + `verified_label=real/fake` when override verdict is authentic/fake/likely_fake. Without this, retrain query returned 0 samples even after manual review. Plus retrain task: accepts `triggered_by_id` (FK on RetrainRun, migration 0004) + `min_samples_override`; sends email to triggering user on success/skipped/failed. Frontend gets a "Force" checkbox to lower min_samples to 1 for testing on tiny datasets.
- 2026-06-19: Staff bypass throttles globally (new `common/throttling.py` Staff* subclasses set as DEFAULT_THROTTLE_CLASSES). UploadRateThrottle also bypasses staff. Admins no longer hit override/upload rate caps.
- 2026-06-19: Retrain trigger endpoint added: `POST /api/v1/analyzers/retrain/` (IsAdminUser) + `GET /api/v1/analyzers/retrain/runs/` for last 10. Frontend ReviewQueuePage now has media-type picker + "Run Retrain" button.
- 2026-06-19: Subscription gate Zap icon: text-iris bug (transparent gradient color) → text-violet-300.
- 2026-06-19: Pricing Internal tier: collapsed Active/Staff stack to single badge with priority, pr-16 on header row to prevent label/badge overlap.
- 2026-06-19: Factcheck v2 landed: Wikipedia lookup, confidence score per claim, top-3 web sources, claim->source highlight panel, PDF export, URL input mode, PDF/DOCX upload, qwen2.5:7b optional. See [[services/factcheck]].
- 2026-06-19: `backend/common/url_safety.py:validate_public_url` factored from `AnalyzeUrlView`; reused by factcheck URL fetch. SSRF guard now shared.
- 2026-06-19: Memory bank moved to Obsidian vault `claude-memory/` (was `~/.claude/projects/.../memory/`). Local dir frozen.
- 2026-06-18: Redis configured as Django cache backend (was LocMemCache - broke cross-process cache.get). See [[fixes/factcheck-cache]].
- 2026-06-18: Factcheck made async (Celery task + Redis stage cache). POST returns task_id, GET /status/ polls. See [[api/endpoints]].
- 2026-06-18: Resend switched to HTTP API (django-anymail) - no MX records needed, freed for Cloudflare Email Routing. See [[services/auth-email]].
- 2026-06-18: Staff/superuser bypass: no upload limits, skip IsVerifiedUser, skip SubscriptionGate. See [[services/backend]].
- 2026-06-18: toggle-public URL fixed (frontend sent underscore, backend expects hyphen). See [[fixes/toggle-public-url]].
- 2026-06-18: Pricing page: Internal tier card (staff only), Pro checkmarks fixed (text-iris = transparent), staff isCurrent logic.
- 2026-06-17: Browser extension built (MV3, Chrome + Firefox). See [[services/extension]].
- 2026-06-17: `analyze-url` endpoint added + `SubmissionStatusView` (AllowAny). See [[api/endpoints]].
- 2026-06-17: `AnonymousQuota` model - 5/day per IP for analyze-url. See [[models/AnonymousQuota]].
- 2026-06-17: Email service = Resend HTTP API. See [[services/auth-email]].

## Current dev state

- 12GB Docker ceiling. `qwen2.5vl:3b` active. `qwen2.5vl:7b` pulled but OOMs.
- AI ensemble = 2 models (dima806 + umm-maybe), re-seeded 2026-08-19 as `ai_detector` (was deactivated dead code).
- Worker capped at `--max-memory-per-child=2500000`.
- Ollama `MAX_LOADED_MODELS=1`, `KEEP_ALIVE=2m`.
- All UI text uses ASCII hyphen, no em-dashes.
- Production at homelab Ubuntu server (192.168.8.112). Deploy: `~/deploy.sh prooflayer`.
- Docker project name: `deploy`. Containers: `deploy-backend-1`, `deploy-celery_worker-1`, etc.

## Extension status

Chrome/Firefox extension built and tested. Two open issues:
1. Login flow: `btn-login` opens `prooflayer.cloud/login` but JWT not persisted back to extension storage after login.
2. Anonymous quota: 5/day per IP drains fast during dev (each test = 1 use).

## Pending overhaul

[[concepts/detection-strategy-2026]] - 8-week roadmap to replace VLM-only pipeline with calibrated NPR + UniversalFakeDetect + ELA + perceptual hash + EXIF + C2PA + Qwen-as-explainer.

## Key files

- Pipeline dispatch: `backend/analyzers/tasks.py:dispatch_analysis`
- Aggregator: `backend/analyzers/aggregator.py:aggregate`
- Worker tuning: `docker-compose.yml:celery_worker.command`
- Frontend layout: `frontend/src/components/Layout.tsx`
- Shader: `frontend/src/components/ui/ShaderBackground.tsx`
- Status endpoint: `backend/api/system_views.py`

## Memory store

- User memory: `~/.claude/projects/-Users-sevastyan0107-PycharmProjects-prooflayer/memory/`
- Includes: no em-dashes, EU regions, celery app load rule, ollama Railway setup
